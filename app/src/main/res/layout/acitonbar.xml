<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <TextView
        android:layout_centerInParent="true"
        android:id="@+id/shoppingcat_num"
        android:textColor="@color/textcolor_selected"
        android:textSize="20sp"
        android:text="(0)"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:layout_toLeftOf="@id/shoppingcat_num"
        android:layout_centerVertical="true"
        android:layout_marginLeft="16dp"
        android:textColor="@color/tab_main_color"
        android:textSize="20sp"
        android:text="购物车"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

        <Button
            android:layout_alignParentRight="true"
            android:textSize="16sp"
            android:background="@drawable/buttom_compile"
            android:textColor="@color/viewfinder_mask"
            android:text="@string/actionBar_edit"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            android:id="@+id/actionBar_edit"
            android:layout_width="50dp"
            android:layout_height="wrap_content" />
</RelativeLayout>